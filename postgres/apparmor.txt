#include <tunables/global>

profile postgres flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability,
  file,
  signal,
  network tcp,
  network udp,
  network inet,
  network inet6,
  network unix dgram,
  network unix stream,
  network netlink raw,

  capability setgid,
  capability setuid,
  capability dac_read_search,
  capability dac_override,
  capability fowner,
  capability fsetid,
  capability chown,

  # S6-Overlay
  /init rix,
  /bin/** rix,
  /usr/bin/** rix,
  /usr/local/bin/** rix,
  /usr/libexec/** mrix,
  /run/{s6,s6-rc*,service}/** rix,
  /package/** rix,
  /command/** rix,
  /etc/s6/** rix,
  /run/s6/** rix,
  /etc/s6-overlay/{,**} rix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /var/run/{,**} mrwkl,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** rix,
  /tmp/** mrwk,

  # PostgreSQL data, shared resources and packaged files
  /data/** mrwk,
  /dev/shm/** mrwk,
  /usr/share/postgresql*/** r,
  /usr/share/zoneinfo/** r,

  /etc/nsswitch.conf r,
  /etc/hosts r,
  /etc/localtime r,
  /etc/passwd r,
  /etc/group r,
  /proc/sys/kernel/** r,
  /proc/*/oom_score_adj rw,
  /sys/kernel/mm/transparent_hugepage/** r,
}

#include <tunables/global>

profile error_pages flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability,
  file,
  signal,

  network tcp,
  network udp,
  network inet,
  network inet6,
  network unix dgram,
  network unix stream,
  network netlink raw,

  capability setgid,
  capability setuid,
  capability net_bind_service,

  # S6-Overlay
  /init rix,
  /bin/** rix,
  /usr/bin/** rix,
  /usr/local/bin/** rix,
  /run/{s6,s6-rc*,service}/** rix,
  /etc/s6-overlay/{,**} rix,
  /package/** rix,
  /command/** rix,
  /etc/s6/** rix,
  /run/s6/** rix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /var/run/{,**} mrwkl,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** rix,
  /tmp/** mrwk,

  # error-pages binary
  /usr/bin/error-pages rix,

  /etc/nsswitch.conf r,
  /etc/hosts r,
  /etc/localtime r,
  /usr/share/zoneinfo/** r,
  /proc/sys/net/** r,
}
